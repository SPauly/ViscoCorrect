#!/bin/bash
#Change every name to the one in costum-config
#rename proto file + remove existing compiled protofiles if existent
#check if extern grpc should be used
    #change include directories for vscode to extern grpc